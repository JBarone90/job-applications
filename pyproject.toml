[project]
name = "job-applications"
version = "0.1.0"
description = "Tooling for Jacopo Barone's job applications (CV + civil service behaviours)"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11.9",
    "python-docx>=1.2.0",
]
